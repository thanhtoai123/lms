# Lộ trình

| Giai đoạn | Phạm vi | Định nghĩa xong |
|---|---|---|
| **1. Nền tảng + Academics + Teacher app** (repo này) | Monorepo, core rules + test, schema, tRPC, Teacher app (Hôm nay → điểm danh → nhận xét → hoàn tất), Ops (hàng đợi, lớp, buổi, mở lớp), migrate script, CI | GV chốt được buổi trong 1 màn; hàng đợi quá hạn tự giảm; CI xanh |
| **2. Admissions + Engagement** (đã có, CI #11) | 10 trạng thái lead (có "Đang học thử"), LeadChild, chia lead 3 chế độ + bảng sale + đặt lại lượt, bàn giao hàng loạt, chuyển lead liên cơ sở, lead lâu chưa chăm, chốt hàng loạt (tài khoản PH chờ kích hoạt, consent ảnh), SLA/tham số theo cơ sở, Kanban + CRM summary, outbox + rule engine + worker, việc chăm sóc, thông báo nội bộ, form web công khai | CRM cũ tắt được (còn: import Excel, OTP Zalo kích hoạt PH, affiliate/hoa hồng, ZNS thật) |
| 3. Lớp/Buổi nâng cao (ADMIN-SPEC §15 dòng 1–2) | Lịch nhiều giai đoạn + "áp lịch mới"/"kiểm tra lệch lịch", duyệt mở lớp, loại buổi, trợ giảng, checklist, nhận xét + điểm từng HS, học bạ năng lực theo tiêu chí (mốc 5/12), ảnh lớp có duyệt + consent + signed URL, lớp Trial | Giáo vụ + GV vận hành hoàn toàn trên hệ mới |
| 4. Finance + People | Ledger bất biến, đơn/kế hoạch trả góp/QR/SePay đối khớp, sale ghi nhận → kế toán xác nhận, công nợ, hoàn theo buổi, hoa hồng, MISA; chấm công, đơn từ, kỳ công | Module tài chính cũ tắt được |
| 5. LMS + Parent app + Public site + Báo cáo | Giáo trình/bài học/bài tập/SCORM, kho, CMS, nối `sata-ui` vào backend thật (PWA, push), 9 báo cáo, cài đặt vận hành, DSAR | Cut-over hoàn tất, hệ cũ read-only |

## Tiến độ Giai đoạn 3 (khu quản trị mới, menu giống admin.satarobo.vn)

| Đợt | Nội dung | Trạng thái |
|---|---|---|
| 3A | Học viên, tài khoản PH (mã kích hoạt, khoá), đăng ký học (bảo lưu ≤ 3 tháng, đổi gói), chuyển lớp / cơ sở, sắp hết khoá, cơ sở, phòng học | Xong |
| 3B | Lịch tổng theo tuần, điểm danh theo lớp (sửa hồi tố có lý do + báo GV), học bù, cảnh báo rủi ro | Xong |
| 3C | Tiêu chí học bạ, học bạ theo mốc (GV viết → duyệt → gửi PH), sổ học bạ, hoàn thành khoá + chứng chỉ, ảnh lớp (upload, consent, duyệt, signed URL) | Xong |
| 3D | **Lớp Trial** (xếp khách vào buổi có sẵn, kiểm tra chỗ + trần lượt thử, khoá chống tranh chỗ, đổi lịch/huỷ có lý do + báo GV, GV ghi có đến/không đến ngay trong màn buổi học, kết quả đẩy trạng thái lead và tạo việc gọi chốt); **Tài khoản** (tạo, cấp/gỡ vai trò theo cơ sở, khoá/mở có lý do, không tự khoá, luôn còn ≥ 1 Quản trị tối cao); **Vai trò & quyền** (ma trận đọc từ policy engine); **Audit Log** (lọc theo phân hệ / đối tượng / người / ngày, xem trước → sau); **Báo cáo Lead / trải nghiệm / đào tạo / hiệu suất GV** (lọc kỳ + cơ sở theo quyền, xuất CSV) | Xong |
| 3E | **Duyệt mở lớp** (Nháp → Chờ duyệt → Tuyển sinh tự sinh buổi → Đang chạy → Kết thúc; giáo vụ gửi, quản lý cơ sở duyệt; trả về / huỷ có lý do; sĩ số tối thiểu); **lịch nhiều giai đoạn + Áp lịch mới** (xem trước, chỉ dời buổi chưa diễn ra, giữ tổng buổi, bỏ ngày nghỉ, chặn trùng phòng/GV, báo GV + PH); **Kiểm tra lịch buổi học**; **buổi ngoài lộ trình** (coach 1-1/1-2/1-4, bù, vượt, bổ sung — đánh số riêng); **trợ giảng + đổi GV chính áp cho buổi sắp tới**; **checklist trước/sau buổi**, bắt đầu buổi, đánh giá sao từng HV, ghi chú nội bộ | Xong |
| 3F | **Giáo viên** (hồ sơ, ngạch, HĐ, khoá được dạy — chặn phân lớp sai khoá/GV đang nghỉ, tải tuần so định mức, đã dạy theo tháng, đánh giá dự giờ, gắn tài khoản, đổi trạng thái cần bàn giao); **Ngày nghỉ** (toàn hệ thống / theo cơ sở, xem trước buổi bị ảnh hưởng, tự dời buổi theo lịch lớp); **Khoá học**, **Khoá tiên quyết** (chặn ghi danh / chuyển lớp / chốt lead khi thiếu, quản lý cơ sở miễn kèm lý do), **Chương trình học** (phiên bản, nháp → đang dùng, bài học, nhân bản) | Xong |
| 4A | **Tài chính**: phương thức thanh toán theo cơ sở (QR VietQR), **đơn hàng** từ ghi danh (giảm giá bắt buộc ghi chú, trả góp 1–4 kỳ, mã đơn + nội dung CK), **thanh toán 2 vai** (sale ghi nhận → kế toán xác nhận/điều chỉnh/từ chối, số phiếu thu liên tục, in phiếu thu), **sổ cái bất biến**, **công nợ** theo tuổi nợ + sắp đến hạn, **thiếu học phí** (chưa có đơn / chưa đóng đủ), **hoàn tiền** theo buổi chưa học (đề xuất → quản lý duyệt → kế toán chi), CCCD che, xem cần lý do + nhật ký | Xong |
| 4B | **Biến động số dư** (webhook SePay có khoá API, chống trùng; tự khớp đơn theo mã trong nội dung CK → tự xác nhận + cấp phiếu thu, khớp luôn khoản sale đã ghi nhận; nghi vấn → "cần kiểm tra"; khớp tay / bỏ qua có lý do; nhập sao kê CSV), **Nhập giao dịch cũ** (CSV theo mã đơn hoặc mã HV + lớp, xem trước từng dòng, chống trùng theo số phiếu cũ, tự lập đơn), **Hoa hồng** (quy tắc theo cơ sở/loại đơn/hiệu lực, tạm tính khi đơn thu đủ, duyệt → chi tách người, hoàn tiền giảm/thu hồi) | Xong |
| 4C | **Nhân sự** (hồ sơ, mã NV, gắn tài khoản / GV, lương–CCCD–STK chỉ HR/Kế toán, xem đầy đủ cần lý do; nghỉ việc tự kết thúc vị trí, gỡ ca, huỷ đơn), **Vị trí công việc** (chính / kiêm nhiệm / uỷ quyền ≤ 90 ngày, chặn trùng vị trí chính), **Phân ca** (ca mẫu, lưới tuần, chép tuần, toạ độ + bán kính cơ sở), **Chấm công** (GPS trong bán kính, bảng công tháng: muộn / sớm / vắng / thiếu chấm / phép / lễ / OT, chỉnh công có lý do, khoá kỳ khi hết đơn chờ, mở lại chỉ Hội sở), **Đơn từ** (nghỉ phép có số dư phép năm, đi muộn / về sớm, làm thêm, quên chấm công → thêm lượt chấm khi duyệt; không tự duyệt), **Của tôi** (chấm vào/ra, lịch ca 2 tuần, công tháng, làm đơn) | Xong |
| 5A | **CSKH phụ huynh**: Yêu cầu PH (7 loại, hạn xử lý theo loại, giao việc, duyệt xin nghỉ → ghi sẵn vắng có phép, duyệt học bù → tạo yêu cầu xếp buổi, liên kết bảo lưu / chuyển lớp / hoàn tiền), Đánh giá PH (sao chung + GV, nhãn, ≤ 2 sao tự tạo việc gọi lại, phản hồi gửi PH, thống kê theo GV), Khảo sát / NPS (trình soạn câu hỏi, gửi theo lớp / cơ sở, tự gửi sau buổi N / hoàn thành khoá, trang trả lời công khai /ks/…, NPS 0–6 tạo việc chăm sóc), Thông báo PH (nhật ký gửi, gửi hàng loạt có biến và xem trước, gửi lại lỗi), Sinh nhật HV (lời chúc 1 lần/năm) | Xong |
| 5B | **Hệ thống**: mẫu email theo sự kiện (biến kiểm tra, xem trước, gửi thử, khôi phục mặc định), nhật ký email (hàng đợi, Resend khi có khoá, thử lại 3 lần), OTP (băm, hết hạn 5 phút, giới hạn theo SĐT/IP, nhật ký che số), nhóm người dùng (thành viên, thông báo nội bộ hàng loạt), cây tổ chức (khu vực → cơ sở → bộ phận), chạy lại webhook (mọi lần gọi SePay / form công khai được ghi), tích hợp (trạng thái dịch vụ, không lộ khoá), cài đặt (pháp nhân, MST, chân phiếu thu); **Báo cáo trung tâm** (thu / hoàn / thuần, đơn, ghi danh, chuyên cần, công nợ theo cơ sở × tháng), **Doanh thu vs mục tiêu** (Hội sở đặt mục tiêu tháng, % đạt, tiến độ tháng hiện tại, YTD); phiếu thu xác nhận tự gửi email nếu có email KH | Xong |
| 5C | **Kho & học cụ**: danh mục hàng (linh kiện / bộ học cụ / sản phẩm / vật tư, giá bán – thuê – cọc, tồn tối thiểu), định mức bộ học cụ + đóng bộ từ linh kiện, tồn theo cơ sở với giá vốn bình quân, phiếu nhập / cấp phát / nhận lại / báo hỏng / chuyển kho (sổ chỉ thêm, tồn không âm), bán sản phẩm tự lập đơn + xuất kho (huỷ đơn tự nhập lại), cho thuê có hạn, cọc, trừ trễ hạn / hỏng / mất, kiểm kê (đếm → nộp → người khác duyệt → tự điều chỉnh, chênh lệch lớn bắt buộc nguyên nhân); **SataCoin**: sổ xu chỉ thêm, hạn mức theo vai trò (GV 20 xu/lần, 50 xu/HV/ngày, chỉ HV lớp mình), thưởng hàng loạt / cả buổi theo điểm danh, điều chỉnh & thu hồi có lý do (≤ 30 ngày), hạng Đồng → Kim cương, đổi quà (giữ xu → quản lý duyệt trừ xu → trao quà trừ kho, huỷ hoàn xu) | Xong |
| 5D | **Học liệu**: tài liệu giảng dạy theo khoá / bài (tệp, liên kết, video nhúng; phiên bản không ghi đè, nháp → phát hành → lưu trữ, GV chỉ thấy khoá mình dạy, tải qua URL có chữ ký, nhật ký mở / tải); **SCORM 1.2 / 2004** (giải nén an toàn, đọc imsmanifest, trình chạy có API runtime, lưu tiến độ / điểm / thời gian, báo cáo người học); **Tài liệu lớp tôi** (theo bài + buổi sắp tới); **Bài tập về nhà** (mẫu theo khoá, giao theo lớp → tạo bài nộp + báo PH, PH nộp qua link riêng không cần đăng nhập, nộp muộn / hết hạn, GV nộp hộ, chấm – trả lại – miễn – đóng bài, thưởng xu khi ≥ 80%, nhắc hạn 24h); **Đề xuất sửa giáo án** (GV gửi thay đổi tên bài / mục tiêu / học cụ, Đào tạo xem xét – duyệt – áp dụng, không tự duyệt, phát hiện xung đột, trao đổi) | Xong |
| 5E | **Website**: tin tức (markdown an toàn, SEO, nháp → hẹn giờ → phát hành → gỡ / lưu trữ, slug cố định sau khi đăng, trang /tin-tuc công khai + API cho site ngoài), nội dung từng trang (khối có kiểm tra, khoá phiên bản chống ghi đè, lịch sử & khôi phục, ảnh kiểm tra chữ ký tệp); **Tracking marketing** (sự kiện first-party ẩn danh không lưu IP, UTM → lead, form_submit gắn lead, cấu hình Pixel / GA4, trình tạo link UTM, chiến dịch + chi phí theo ngày, CPL / CPA / ROAS, vượt ngân sách), **Phễu marketing** 10 bước; **Cohort** theo tháng nhập học (đường cong giữ chân M0–M6, theo khoá), **Churn** (theo tháng, lý do nhóm tự động, cơ sở / khoá, nghỉ sớm ≤ 4 buổi); **Tuân thủ NĐ13**: sổ đồng ý chỉ thêm (dịch vụ / tiếp thị / hình ảnh, phiên bản điều khoản), form đăng ký có ô đồng ý tiếp thị, yêu cầu của chủ thể dữ liệu (6 loại, hạn 72 giờ, cơ sở ghi nhận → DPO xác minh – xử lý, xuất bản sao JSON, rút đồng ý, ẩn danh hoá có mã xác nhận, giữ chứng từ kế toán), tự ẩn danh lead quá hạn lưu giữ, thống kê truy cập dữ liệu cá nhân | Xong |
| 5F | Tuyển dụng (/jobs), hội thoại & Messenger CRM (/hoi-thoai), tin nhắn, cộng tác viên / affiliate (/affiliates), báo cáo chat-pilot | Tiếp theo |

## Việc kỹ thuật còn lại trong Giai đoạn 1 (sau khi CI xanh)

1. Supabase MFA cho SUPER_ADMIN/HO_*; middleware `proxy.ts` chặn route theo role.
2. `session_media` upload qua presigned URL (R2) + duyệt ảnh + lọc theo `media_consent`.
3. Offline queue cho điểm danh (IndexedDB + background sync) trong Teacher PWA.
4. Bảng `outbox` + worker (Inngest/Trigger.dev) phát sự kiện `session.completed` → thông báo PH.
5. RLS policies sinh từ policy engine (script) làm lớp phòng thủ cuối.
6. Trang Ops: học viên, ghi danh (UI cho `classes.enroll`), đổi lịch (đóng rule cũ + sinh lại buổi tương lai).
