# Lộ trình

| Giai đoạn | Phạm vi | Định nghĩa xong |
|---|---|---|
| **1. Nền tảng + Academics + Teacher app** (repo này) | Monorepo, core rules + test, schema, tRPC, Teacher app (Hôm nay → điểm danh → nhận xét → hoàn tất), Ops (hàng đợi, lớp, buổi, mở lớp), migrate script, CI | GV chốt được buổi trong 1 màn; hàng đợi quá hạn tự giảm; CI xanh |
| **2. Admissions + Engagement** (đã có, CI #11) | 10 trạng thái lead (có "Đang học thử"), LeadChild, chia lead 3 chế độ + bảng sale + đặt lại lượt, bàn giao hàng loạt, chuyển lead liên cơ sở, lead lâu chưa chăm, chốt hàng loạt (tài khoản PH chờ kích hoạt, consent ảnh), SLA/tham số theo cơ sở, Kanban + CRM summary, outbox + rule engine + worker, việc chăm sóc, thông báo nội bộ, form web công khai | CRM cũ tắt được (còn: import Excel, OTP Zalo kích hoạt PH, affiliate/hoa hồng, ZNS thật) |
| 3. Lớp/Buổi nâng cao (ADMIN-SPEC §15 dòng 1–2) | Lịch nhiều giai đoạn + "áp lịch mới"/"kiểm tra lệch lịch", duyệt mở lớp, loại buổi, trợ giảng, checklist, nhận xét + điểm từng HS, học bạ năng lực theo tiêu chí (mốc 5/12), ảnh lớp có duyệt + consent + signed URL, lớp Trial | Giáo vụ + GV vận hành hoàn toàn trên hệ mới |
| 4. Finance + People | Ledger bất biến, đơn/kế hoạch trả góp/QR/SePay đối khớp, sale ghi nhận → kế toán xác nhận, công nợ, hoàn theo buổi, hoa hồng, MISA; chấm công, đơn từ, kỳ công | Module tài chính cũ tắt được |
| 5. LMS + Parent app + Public site + Báo cáo | Giáo trình/bài học/bài tập/SCORM, kho, CMS, nối `sata-ui` vào backend thật (PWA, push), 9 báo cáo, cài đặt vận hành, DSAR | Cut-over hoàn tất, hệ cũ read-only |

## Tiến độ Giai đoạn 3 (khu quản trị mới, menu giống admin.satarobo.vn)

| Đợt | Nội dung | Trạng thái |
|---|---|---|
| 3A | Học viên, tài khoản PH (mã kích hoạt, khoá), đăng ký học (bảo lưu ≤ 3 tháng, đổi gói), chuyển lớp / cơ sở, sắp hết khoá, cơ sở, phòng học | Xong |
| 3B | Lịch tổng theo tuần, điểm danh theo lớp (sửa hồi tố có lý do + báo GV), học bù, cảnh báo rủi ro | Xong |
| 3C | Tiêu chí học bạ, học bạ theo mốc (GV viết → duyệt → gửi PH), sổ học bạ, hoàn thành khoá + chứng chỉ, ảnh lớp (upload, consent, duyệt, signed URL) | Xong |
| 3D | **Lớp Trial** (xếp khách vào buổi có sẵn, kiểm tra chỗ + trần lượt thử, khoá chống tranh chỗ, đổi lịch/huỷ có lý do + báo GV, GV ghi có đến/không đến ngay trong màn buổi học, kết quả đẩy trạng thái lead và tạo việc gọi chốt); **Tài khoản** (tạo, cấp/gỡ vai trò theo cơ sở, khoá/mở có lý do, không tự khoá, luôn còn ≥ 1 Quản trị tối cao); **Vai trò & quyền** (ma trận đọc từ policy engine); **Audit Log** (lọc theo phân hệ / đối tượng / người / ngày, xem trước → sau); **Báo cáo Lead / trải nghiệm / đào tạo / hiệu suất GV** (lọc kỳ + cơ sở theo quyền, xuất CSV) | Xong |
| 3E | **Duyệt mở lớp** (Nháp → Chờ duyệt → Tuyển sinh tự sinh buổi → Đang chạy → Kết thúc; giáo vụ gửi, quản lý cơ sở duyệt; trả về / huỷ có lý do; sĩ số tối thiểu); **lịch nhiều giai đoạn + Áp lịch mới** (xem trước, chỉ dời buổi chưa diễn ra, giữ tổng buổi, bỏ ngày nghỉ, chặn trùng phòng/GV, báo GV + PH); **Kiểm tra lịch buổi học**; **buổi ngoài lộ trình** (coach 1-1/1-2/1-4, bù, vượt, bổ sung — đánh số riêng); **trợ giảng + đổi GV chính áp cho buổi sắp tới**; **checklist trước/sau buổi**, bắt đầu buổi, đánh giá sao từng HV, ghi chú nội bộ | Xong |
| 3F | **Giáo viên** (hồ sơ, ngạch, HĐ, khoá được dạy — chặn phân lớp sai khoá/GV đang nghỉ, tải tuần so định mức, đã dạy theo tháng, đánh giá dự giờ, gắn tài khoản, đổi trạng thái cần bàn giao); **Ngày nghỉ** (toàn hệ thống / theo cơ sở, xem trước buổi bị ảnh hưởng, tự dời buổi theo lịch lớp); **Khoá học**, **Khoá tiên quyết** (chặn ghi danh / chuyển lớp / chốt lead khi thiếu, quản lý cơ sở miễn kèm lý do), **Chương trình học** (phiên bản, nháp → đang dùng, bài học, nhân bản) | Xong |
| 4A | **Tài chính**: phương thức thanh toán theo cơ sở (QR VietQR), **đơn hàng** từ ghi danh (giảm giá bắt buộc ghi chú, trả góp 1–4 kỳ, mã đơn + nội dung CK), **thanh toán 2 vai** (sale ghi nhận → kế toán xác nhận/điều chỉnh/từ chối, số phiếu thu liên tục, in phiếu thu), **sổ cái bất biến**, **công nợ** theo tuổi nợ + sắp đến hạn, **thiếu học phí** (chưa có đơn / chưa đóng đủ), **hoàn tiền** theo buổi chưa học (đề xuất → quản lý duyệt → kế toán chi), CCCD che, xem cần lý do + nhật ký | Xong |
| 4B | **Biến động số dư** (webhook SePay có khoá API, chống trùng; tự khớp đơn theo mã trong nội dung CK → tự xác nhận + cấp phiếu thu, khớp luôn khoản sale đã ghi nhận; nghi vấn → "cần kiểm tra"; khớp tay / bỏ qua có lý do; nhập sao kê CSV), **Nhập giao dịch cũ** (CSV theo mã đơn hoặc mã HV + lớp, xem trước từng dòng, chống trùng theo số phiếu cũ, tự lập đơn), **Hoa hồng** (quy tắc theo cơ sở/loại đơn/hiệu lực, tạm tính khi đơn thu đủ, duyệt → chi tách người, hoàn tiền giảm/thu hồi) | Xong |
| 4C | **Nhân sự** (hồ sơ, mã NV, gắn tài khoản / GV, lương–CCCD–STK chỉ HR/Kế toán, xem đầy đủ cần lý do; nghỉ việc tự kết thúc vị trí, gỡ ca, huỷ đơn), **Vị trí công việc** (chính / kiêm nhiệm / uỷ quyền ≤ 90 ngày, chặn trùng vị trí chính), **Phân ca** (ca mẫu, lưới tuần, chép tuần, toạ độ + bán kính cơ sở), **Chấm công** (GPS trong bán kính, bảng công tháng: muộn / sớm / vắng / thiếu chấm / phép / lễ / OT, chỉnh công có lý do, khoá kỳ khi hết đơn chờ, mở lại chỉ Hội sở), **Đơn từ** (nghỉ phép có số dư phép năm, đi muộn / về sớm, làm thêm, quên chấm công → thêm lượt chấm khi duyệt; không tự duyệt), **Của tôi** (chấm vào/ra, lịch ca 2 tuần, công tháng, làm đơn) | Xong |
| 5A | **CSKH phụ huynh**: Yêu cầu PH (7 loại, hạn xử lý theo loại, giao việc, duyệt xin nghỉ → ghi sẵn vắng có phép, duyệt học bù → tạo yêu cầu xếp buổi, liên kết bảo lưu / chuyển lớp / hoàn tiền), Đánh giá PH (sao chung + GV, nhãn, ≤ 2 sao tự tạo việc gọi lại, phản hồi gửi PH, thống kê theo GV), Khảo sát / NPS (trình soạn câu hỏi, gửi theo lớp / cơ sở, tự gửi sau buổi N / hoàn thành khoá, trang trả lời công khai /ks/…, NPS 0–6 tạo việc chăm sóc), Thông báo PH (nhật ký gửi, gửi hàng loạt có biến và xem trước, gửi lại lỗi), Sinh nhật HV (lời chúc 1 lần/năm) | Xong |
| 5B | **Hệ thống**: mẫu email theo sự kiện (biến kiểm tra, xem trước, gửi thử, khôi phục mặc định), nhật ký email (hàng đợi, Resend khi có khoá, thử lại 3 lần), OTP (băm, hết hạn 5 phút, giới hạn theo SĐT/IP, nhật ký che số), nhóm người dùng (thành viên, thông báo nội bộ hàng loạt), cây tổ chức (khu vực → cơ sở → bộ phận), chạy lại webhook (mọi lần gọi SePay / form công khai được ghi), tích hợp (trạng thái dịch vụ, không lộ khoá), cài đặt (pháp nhân, MST, chân phiếu thu); **Báo cáo trung tâm** (thu / hoàn / thuần, đơn, ghi danh, chuyên cần, công nợ theo cơ sở × tháng), **Doanh thu vs mục tiêu** (Hội sở đặt mục tiêu tháng, % đạt, tiến độ tháng hiện tại, YTD); phiếu thu xác nhận tự gửi email nếu có email KH | Xong |
| 5C | **Kho & học cụ**: danh mục hàng (linh kiện / bộ học cụ / sản phẩm / vật tư, giá bán – thuê – cọc, tồn tối thiểu), định mức bộ học cụ + đóng bộ từ linh kiện, tồn theo cơ sở với giá vốn bình quân, phiếu nhập / cấp phát / nhận lại / báo hỏng / chuyển kho (sổ chỉ thêm, tồn không âm), bán sản phẩm tự lập đơn + xuất kho (huỷ đơn tự nhập lại), cho thuê có hạn, cọc, trừ trễ hạn / hỏng / mất, kiểm kê (đếm → nộp → người khác duyệt → tự điều chỉnh, chênh lệch lớn bắt buộc nguyên nhân); **SataCoin**: sổ xu chỉ thêm, hạn mức theo vai trò (GV 20 xu/lần, 50 xu/HV/ngày, chỉ HV lớp mình), thưởng hàng loạt / cả buổi theo điểm danh, điều chỉnh & thu hồi có lý do (≤ 30 ngày), hạng Đồng → Kim cương, đổi quà (giữ xu → quản lý duyệt trừ xu → trao quà trừ kho, huỷ hoàn xu) | Xong |
| 5D | **Học liệu**: tài liệu giảng dạy theo khoá / bài (tệp, liên kết, video nhúng; phiên bản không ghi đè, nháp → phát hành → lưu trữ, GV chỉ thấy khoá mình dạy, tải qua URL có chữ ký, nhật ký mở / tải); **SCORM 1.2 / 2004** (giải nén an toàn, đọc imsmanifest, trình chạy có API runtime, lưu tiến độ / điểm / thời gian, báo cáo người học); **Tài liệu lớp tôi** (theo bài + buổi sắp tới); **Bài tập về nhà** (mẫu theo khoá, giao theo lớp → tạo bài nộp + báo PH, PH nộp qua link riêng không cần đăng nhập, nộp muộn / hết hạn, GV nộp hộ, chấm – trả lại – miễn – đóng bài, thưởng xu khi ≥ 80%, nhắc hạn 24h); **Đề xuất sửa giáo án** (GV gửi thay đổi tên bài / mục tiêu / học cụ, Đào tạo xem xét – duyệt – áp dụng, không tự duyệt, phát hiện xung đột, trao đổi) | Xong |
| 5E | **Website**: tin tức (markdown an toàn, SEO, nháp → hẹn giờ → phát hành → gỡ / lưu trữ, slug cố định sau khi đăng, trang /tin-tuc công khai + API cho site ngoài), nội dung từng trang (khối có kiểm tra, khoá phiên bản chống ghi đè, lịch sử & khôi phục, ảnh kiểm tra chữ ký tệp); **Tracking marketing** (sự kiện first-party ẩn danh không lưu IP, UTM → lead, form_submit gắn lead, cấu hình Pixel / GA4, trình tạo link UTM, chiến dịch + chi phí theo ngày, CPL / CPA / ROAS, vượt ngân sách), **Phễu marketing** 10 bước; **Cohort** theo tháng nhập học (đường cong giữ chân M0–M6, theo khoá), **Churn** (theo tháng, lý do nhóm tự động, cơ sở / khoá, nghỉ sớm ≤ 4 buổi); **Tuân thủ NĐ13**: sổ đồng ý chỉ thêm (dịch vụ / tiếp thị / hình ảnh, phiên bản điều khoản), form đăng ký có ô đồng ý tiếp thị, yêu cầu của chủ thể dữ liệu (6 loại, hạn 72 giờ, cơ sở ghi nhận → DPO xác minh – xử lý, xuất bản sao JSON, rút đồng ý, ẩn danh hoá có mã xác nhận, giữ chứng từ kế toán), tự ẩn danh lead quá hạn lưu giữ, thống kê truy cập dữ liệu cá nhân | Xong |
| 5F | **Tuyển dụng** (tin đăng công khai /tuyen-dung, nộp CV PDF/DOCX kiểm tra chữ ký tệp + đồng ý xử lý dữ liệu, sàng lọc → phỏng vấn có chấm điểm, không đề nghị khi chưa chấm, chống trùng lịch người phỏng vấn, nhận việc tự tạo hồ sơ nhân sự thử việc, tự đóng tin khi đủ người, ẩn danh hồ sơ trượt sau 12 tháng); **Hộp thư đa kênh** (/tin-nhan: liên kết riêng cho phụ huynh, Messenger — cửa sổ 24h / HUMAN_AGENT 7 ngày, Zalo OA tin tư vấn 7 ngày, webhook kiểm tra chữ ký, chống trùng tin, ghi chú nội bộ, giao người, gắn cờ khiếu nại / hoàn tiền / muốn nghỉ / chuyển khoản cá nhân); **Messenger CRM** (tạo lead từ hội thoại có xác nhận đồng ý); **Quản trị hội thoại** (thời gian phản hồi trung vị, đúng hạn 60 phút theo nhân viên / kênh, chờ quá hạn, tin gửi lỗi, cấu hình kênh); **Nguồn giới thiệu** (mã ?ref=, thưởng cố định / % có trần khi đơn đầu thu đủ, duyệt → chi tách người, tự huỷ khi huỷ / hoàn đơn, chặn tự giới thiệu); **Đo pilot chat** (kích hoạt, mở / trả lời, đọc ≤ 48h, tốc độ phản hồi, ngưỡng mở rộng); **Tuân thủ** cập nhật theo NĐ 356/2025 (hạn 10 / 15 / 20 ngày, phản hồi tiếp nhận 2 ngày làm việc, gia hạn 1 lần, sổ sự cố — báo A05 trong 72 giờ); **Vận hành** (/api/health, nhịp worker, kiểm tra biến môi trường, sao lưu / thử khôi phục, danh mục go-live, sổ tay docs/VAN-HANH.md) | Xong |
| 6A | **Hoá đơn điện tử** (/hoa-don): tự lập nháp từ phiếu thu đã xác nhận (thu đủ → theo dòng đơn; thu từng đợt → 1 dòng "Học phí đợt n"), thuế suất theo loại (học phí mặc định không chịu thuế, hàng hoá 10%), kiểm tra ký hiệu theo năm + MST người mua, phát hành qua nhà cung cấp (bộ chuyển HTTP chung; sandbox để thử — không có giá trị pháp lý), số hoá đơn liên tục, mã tra cứu + trang /tra-cuu-hoa-don, email cho người mua, **đã phát hành thì khoá** (trigger CSDL chặn sửa / huỷ / xoá) → chỉ lập **điều chỉnh / thay thế** kèm văn bản thoả thuận, cảnh báo quá hạn lập, khoản thu chưa có hoá đơn, hoàn tiền cần điều chỉnh, đối soát thu – xuất theo tháng | Xong |
| 6B | **Điểm danh QR** (/the-hoc-vien): thẻ QR ký HMAC theo học viên + phiên bản, in theo lớp, cấp lại thẻ (thẻ cũ mất hiệu lực, có lý do + nhật ký); GV quét bằng camera điện thoại (/teacher/sessions/…/quet) hoặc nhập mã — chỉ trong ngày học, đúng lớp, ghi có mặt / đi muộn (quá 15 phút), quét lại không ghi trùng | Xong |
| 6C | **Cổng phụ huynh** (/ph): đăng nhập bằng SĐT + OTP Zalo hoặc mã kích hoạt (không tiết lộ số có tồn tại, giới hạn lần gửi / thử), phiên 30 ngày httpOnly, xem & thu hồi thiết bị; lịch học, chuyên cần, bài tập, học bạ đã gửi, xu, thẻ QR của con; học phí còn nợ + QR chuyển khoản + hoá đơn; tin nhắn với trung tâm (mở câu hỏi mới → giao cơ sở); thông báo; tự đổi đồng ý tiếp thị / hình ảnh | Xong |
| 7A | **Chuyển đổi dữ liệu** (/chuyen-doi): nhập học viên + phụ huynh (giữ mã cũ nếu chưa trùng, gộp phụ huynh theo SĐT, ghép học viên đã tạo tay cùng tên + SĐT), nhập ghi danh mang sang số buổi đã học (điểm danh mới tính từ buổi chưa học kế tiếp), kiểm tra từng dòng trước khi nhập, chạy lại không nhân bản; **đối soát** số liệu tổng (học viên, ghi danh, lớp, buổi còn lại, công nợ, thu tháng) và từng học viên (file buổi còn lại / công nợ từ hệ cũ) | Xong |
| 7B | **Go-live cơ sở** (/go-live): Chuẩn bị → Chạy song song → Chính thức → Hệ cũ chỉ đọc; sổ đối chiếu hằng ngày (điểm danh, tiền thu, ghi danh mới, ghi danh mở), chênh lệch phải giải thích, cần 5 ngày khớp liên tiếp + danh mục (dữ liệu, đối soát, đào tạo, tài khoản, sao lưu đã thử khôi phục) mới chuyển chính thức; Hội sở quyết định, ghi nhật ký | Xong |
| 7C | **Kênh gửi Zalo ZNS + SMS** (Cấu hình vận hành → Tin Zalo): chế độ tắt / giả lập / gửi thật, khai báo mẫu ZNS (template_id + ánh xạ tham số) theo sự kiện, nội dung SMS không dấu có đếm số tin, OTP gửi ngay (ZNS → SMS), hàng đợi thông báo gửi qua worker với giờ yên lặng, trần tin / phụ huynh / ngày, thử lại lỗi tạm thời, ZNS lỗi chuyển SMS, gửi thử | Xong |
| 8A | **Thông báo đẩy cho cổng phụ huynh** (Web Push chuẩn RFC 8291 / 8292, không dùng dịch vụ trung gian): cổng /ph cài được lên màn hình chính, phụ huynh tự bật / tắt theo thiết bị (tối đa 10), mọi thông báo trong app (tin nhắn trung tâm, nhắc học phí, học bạ…) tự đẩy tới điện thoại trong 12 giờ, theo giờ yên lặng, chỉ gửi tới dịch vụ đẩy chính thức (chống SSRF), đăng ký hết hạn tự gỡ | Xong |
| 8B | **Sổ phản hồi pilot** (/go-live → Phản hồi pilot): lỗi / sai dữ liệu / chưa biết thao tác / đề xuất, mức chặn công việc báo ngay Hội sở và phải xử lý trong 4 giờ, chặn khoá hệ cũ khi còn sự cố chặn việc | Xong |
| 8C | **Báo cáo sau go-live** (/bao-cao/sau-go-live): theo tuần và cơ sở — điểm danh chốt trong ngày, tự khớp chuyển khoản, phủ hoá đơn điện tử, phụ huynh dùng cổng 30 ngày, OTP gửi được, tin ZNS / SMS / đẩy thành công, so mục tiêu | Xong |
| 9A | **Hướng dẫn & đào tạo** (/huong-dan): 6 bài theo vai trò (làm quen, giáo viên, giáo vụ, tư vấn / CSKH, kế toán, quản lý go-live), có bước thao tác kèm liên kết và câu hỏi kiểm tra (đúng hết mới hoàn thành); danh mục go-live "nhân sự đã học" tự kiểm tra theo tài khoản có vai trò tại cơ sở | Xong |
| 9B | **Kiểm tra dữ liệu trước pilot** (trong /go-live): 12 mục — phòng, quản lý, kế toán, học viên chưa có phụ huynh, lớp thiếu giáo viên / thiếu buổi, buổi 7 ngày tới thiếu giáo viên, SĐT phụ huynh sai, ghi danh chưa có đơn, học vượt gói, thiếu QR chuyển khoản, giáo viên chưa có tài khoản; mục chặn làm danh mục go-live chưa đạt | Xong |
| 10A | **Cấu hình vận hành đầy đủ** (/cau-hinh-van-hanh): 11 tab đều dùng được — OTP (hiệu lực, số lần sai, chờ gửi lại, trần theo SĐT / IP), Học viên (sắp hết khoá, bảo lưu tối đa, hạn học bù, ngưỡng cảnh báo chuyên cần), Lớp (quét thẻ tính đi muộn), Chấm công (dung sai muộn / sớm), Thanh toán (nhắc đợt mặc định), Nhắc tự động (nhắc hạn bài tập); cơ sở ghi đè hoặc theo mặc định; mỗi thay đổi ghi nhật ký; tham số dùng thật trong nghiệp vụ | Xong |
| 10B | **Điểm danh khi mất mạng** (app giáo viên): nháp điểm danh lưu trên máy, trang buổi học xem lại được khi mất kết nối, bấm lưu khi mất mạng thì xếp hàng và tự gửi khi có mạng (hiện số buổi chờ gửi, gửi ngay) | Xong |
| 11A | **Phiên đăng nhập bền**: tự làm mới phiên Supabase trước khi hết hạn (không bị đăng xuất mỗi giờ), đăng xuất thu hồi phiên, phiên hỏng quay về trang đăng nhập | Xong |
| 11B | **Mời nhân sự / đặt lại mật khẩu**: quản trị gửi email mời đặt mật khẩu từ trang người dùng, nhân sự tự "Quên mật khẩu" (không tiết lộ email có tồn tại, giới hạn số lần), trang đặt mật khẩu dùng liên kết một lần với chính sách mật khẩu (≥10 ký tự, có chữ và số) | Xong |
| 11C | **Xác thực 2 lớp (TOTP)** (/bao-mat): mọi nhân sự tự bật bằng ứng dụng OTP; vai trò trong `REQUIRE_MFA_ROLES` (mặc định Quản trị hệ thống) bắt buộc — chưa xác thực thì mọi thao tác bị chặn ở máy chủ và bị chuyển tới /bao-mat | Xong |
| 11D | **Khoá tài khoản đồng bộ đăng nhập**: khoá trong hệ thống đồng thời cấm tài khoản Supabase (không đăng nhập / làm mới phiên được) | Xong |
| 12A | **Chống dò mật khẩu & nhật ký đăng nhập**: tạm khoá đăng nhập theo tài khoản (mặc định sai 5 lần → 15 phút) và theo IP, không gọi Supabase cho tài khoản đã khoá; nhật ký đăng nhập (đăng nhập, sai mật khẩu, bị chặn, 2 lớp, đặt mật khẩu, đăng xuất, tự đăng xuất) lưu 1 năm, email chỉ lưu dạng băm + che; xem ở /bao-mat, trang người dùng; quản trị mở khoá tạm | Xong |
| 12B | **Tự đăng xuất khi không thao tác** (mặc định 60 phút, cấu hình được): cảnh báo trước 2 phút, đồng bộ giữa các tab, máy chủ kiểm tra độc lập (trang + API); giáo viên đang mất mạng không bị đẩy ra, hàng đợi điểm danh được giữ | Xong |
| 12C | **Bảo mật hệ thống** (/bao-mat-he-thong): khuyến nghị theo mức (tài khoản mẫu bật trên production, quản trị chưa bật 2 lớp, quá nhiều quản trị, tài khoản ngủ > 90 ngày, IP sai nhiều), chính sách đang áp dụng, nhật ký đăng nhập toàn hệ thống. Chặn gọi API từ trang khác (CSRF), bổ sung header bảo mật; sửa lỗi chính sách trình duyệt chặn GPS làm hỏng chấm công | Xong |
| 12D | **Tìm nhanh Ctrl+K**: mở trang (gõ không dấu), tìm học viên / lead / lớp / mã đơn / SĐT theo quyền và cơ sở (SĐT luôn che), trang mở gần đây, điều khiển bằng bàn phím; liên kết "Bỏ qua menu" cho bàn phím | Xong |
| 12E | **Bù khoảng trống so với bản gốc**: Leads lọc theo cơ sở / người phụ trách / nguồn / ngày nhận, xem mọi trạng thái, phân trang + số dòng, cột ngày nhận, xuất CSV; Buổi học lọc theo cơ sở / lớp / giáo viên; Lớp học lọc theo khoá / giáo viên; Đăng ký học lọc theo lớp; Thanh toán có nút Ghi nhận khoản | Xong |
| 13A | **Đợt 1 bám nghiệp vụ gốc — Tuyển sinh → Chốt → Thu** (docs/GAP-NGHIEP-VU.md mục 1, 4, 7, 8, 12, 17, 21, 22): **chặn chốt khi chưa ghi nhận thanh toán** (`orders.lead_id`, `order_items` gắn con / học viên / ghi danh, khối Thanh toán trên lead, `/orders/new?leadId=` khoá cơ sở & SĐT theo khách, đơn 0đ học bổng toàn phần có lý do mới qua cửa); **sửa / xoá lead** (soft-delete có lý do, chặn khi đã chốt / còn đơn), **lý do bắt buộc 3–500 ký tự** khi sang Đang nuôi dưỡng / Đã mất, bổ sung cạnh máy trạng thái, bỏ `enroll` khỏi API `transition` (chốt chỉ qua Chuyển đổi), Kanban đổi trạng thái + nút Phân công; **khử trùng SĐT toàn cục** (mọi trạng thái, `dedupeDays = 0` là mãi mãi) với quy tắc gộp chỉ-điền-ô-trống + thêm con mới + giá trị khác ghi vào ghi chú kèm ngày, đếm `reentry_count`, `facebook_url`, phiếu nhập nhanh ở lại trang kèm “Đã nhập trong phiên”; **sổ lượt chia đúng quy tắc** (chỉ máy chia luân phiên tiêu lượt) qua một `recordAssignment` duy nhất, bảng `lead_distribution_log` + `lead_pool_events`, bật lại / đặt lại lượt về mức thấp nhất, chỉnh lượt có lý do, tab “Sổ chia lead” + trang “Lịch sử thay đổi pool”; **form chuyển đổi đầy đủ** (CCCD mã hoá + địa chỉ phụ huynh, nhiều học viên một lần, học bổng toàn phần) và **mã học viên / mã lớp sinh theo max** thay vì đếm bản ghi; **chuyển lead có note bàn giao ≥10 ký tự**, “Chia lại lead”, phân bổ lại hàng loạt lead nguội; **chốt hàng loạt ghi khoản thu lùi ngày** (`payments.source="backfill"`, chờ kế toán) + đọc token `ĐãĐóng=` / `HạnĐợt2=`; **nhập lead từ file** `/leads/import` và `/leads/import/registered` (đọc CSV / dán từ Excel ngay trên máy, 3 nhóm Mới / Trùng / Lỗi, sửa tại chỗ, cột “Đè”, gán sale không tiêu lượt, chia tự động phần còn lại); sửa lỗi đổi lịch học thử bị tính thêm một lượt | Xong |
| 13B | **Đợt 2 — Tài chính hằng ngày (đối chiếu bản gốc, `docs/GAP-NGHIEP-VU.md` mục 5, 6, 9, 13, 19, 23, 24)**: **Trả góp 1–12 đợt + thu cọc trước** (`buildPlan` / `buildModulePlan` 1–4 học phần cách 30 ngày theo SR.QD.223 / `buildMonthlyPlan` tối đa 12 kỳ theo SR.QD.219 Điều 2, cọc tối đa một và đứng đầu) và **sửa kế hoạch sau khi tạo** (`replaceInstallmentPlan` có lý do + chống ghi đè `expectedUpdatedAt`, đợt đã thu giữ nguyên và không nhỏ hơn phần đã thu, đợt cũ huỷ mềm giữ lịch sử, editor “Tổng các phiếu phải bằng X — đang lệch Y”); **Dòng đơn nhiều con** (mỗi dòng gắn học viên / con của lead / ghi danh, **hình thức lớp** nhóm · Coach 1-1 ×2 · 1-2 ×1,8 · 1-4 ×1,5 với nút “Áp số này vào Đơn giá”, **giảm giá theo dòng** cộng dồn có lý do bắt buộc và trần % theo cấu hình vận hành, `net_amount` + khối **Công nợ theo con** kèm tạo / huỷ đợt riêng cho từng con; mỗi ghi danh chỉ một dòng đơn đang mở); **Khoản thu** (`updatePendingPayment` sửa khoản đang chờ, `adjustConfirmedPayment` sinh **bút toán chênh lệch** + `payment_adjustments`, gắn ghi danh / dòng đơn / link chứng từ, `version` chống ghi đè, **trạng thái đơn suy từ tiền** — đã thu tính cả khoản kế toán chưa đối soát, công nợ phụ huynh vẫn chỉ trừ khoản đã xác nhận — badge + tooltip ở danh sách và chi tiết, xác nhận cả lượt khoản nhập liệu ban đầu); **Biến động số dư** (rót một giao dịch cho **nhiều con** — Khớp đủ / Đang thừa / Còn thiếu, tạo đợt tại chỗ, `bank_tx_allocations`; **gỡ gắn** huỷ khoản do giao dịch tạo và đảo bút toán, trả khoản sale về chờ kế toán, thu hồi hoa hồng tạm tính; **tiền thừa chưa xử lý** không tự hoàn, không tự trừ sang đơn khác); **Hoàn tiền theo vòng đời** (`proposeRefundIfPaid` chuyển vào `finance.ts` tính theo dòng đơn của ghi danh, `refunds.trigger` / `auto`, khối “Tạo đề xuất cho ca chưa có”); **Nhập giao dịch cũ** khớp **mã đơn → mã hệ cũ → SĐT phụ huynh + họ tên** (đọc file ngay trong trình duyệt, trạng thái Sẽ ghi / Đã có tiền — bỏ qua / Cần chọn / Không tìm thấy, chọn tay và “Vẫn ghi”, **gán sale bắt buộc**, mỗi em một đơn với khoản giữ đúng ngày đóng ở trạng thái chờ kế toán); **Công nợ theo ghi danh** (chip Chưa chốt học phí / Đủ tiền — chờ KT / Còn thiếu / Chưa đóng đồng nào / Thu vượt / Đã đóng đủ, “Thiếu — PH đang thấy” vs “Thiếu thật”, tuổi nợ 1–7 / 8–30 / >30 theo cấu hình, **sửa học phí hợp đồng** có lý do + bút toán điều chỉnh, “Ghi học phí cũ” ở `/thieu-hoc-phi`) | Xong |
| 13C | **Đợt 3 — Học vụ (đối chiếu bản gốc, `docs/GAP-NGHIEP-VU.md` mục 3, 10, 11, 16, 18, 25a, 25b)**: **Điều chỉnh / huỷ từng buổi** (`sessions.adjust` đổi ngày / giờ / GV / phòng — lý do ≥5, không lùi quá khứ, chặn trùng phòng–GV, nhớ ngày gốc, báo GV + PH; `sessions.cancel` lý do ≥5 với hai chế độ: *dời bù* = buổi huỷ chuyển sang dải số lưu trữ 5001+, các buổi sau dời lên một nhịp theo lịch và sinh thêm buổi cuối ⇒ giữ đủ tổng buổi, hoặc *không bù*; huỷ + báo học thử, trả yêu cầu học bù về chờ xếp; bỏ `cancel`/`reschedule` khỏi `sessions.transition` — sửa lỗi buổi biến mất khỏi lộ trình); **Hồ sơ học viên đủ trường** (SĐT / email HV, mã nhập tay có kiểm tra & duy nhất, nhóm máu, danh sách dị ứng, địa chỉ tách bảng riêng, ngày đăng ký lần đầu tự điền, đơn vị mong muốn, phụ huynh thứ hai + quan hệ ông/bà, CCCD phụ huynh **mã hoá AES-256-GCM**, xem đầy đủ phải ghi lý do + nhật ký PII; lọc khối lớp, tìm theo tên phụ huynh); **Vòng đời học viên** (bảo lưu tất cả lớp hoặc một lớp, ngày trở lại tuỳ chọn, kết thúc bảo lưu, nghỉ học hẳn — rút mọi ghi danh + huỷ học bù + đề xuất hoàn tiền, kích hoạt lại; bảng `student_pauses` + “Lịch sử bảo lưu”; job nhắc trước ngày trở lại / quá hạn bảo lưu tạo việc chăm sóc); **Chuyển lớp có duyệt** (`class_transfer_requests`: cùng khoá — quản lý miễn kèm lý do, không vượt tiến độ, lớp đầy vào danh sách chờ và được báo khi có chỗ; danh sách lớp đích “x bài · còn N chỗ”; duyệt = chuyển thật mang số buổi còn lại, từ chối / rút yêu cầu); **Huỷ lớp dây chuyền** (xem trước ghi danh – buổi tương lai – học thử – tiền đã thu, huỷ trong một giao dịch: rút ghi danh + đề xuất hoàn tiền, huỷ buổi, huỷ học thử, báo GV + PH; bỏ chặn “lớp còn học viên”); **Hoàn tất buổi đúng bản gốc** (`completionBlockers`: điểm danh đủ, xác nhận bài đã dạy, nhận xét chung, nhận xét từng HS có mặt và ảnh lớp theo cấu hình `sessionRequireStudentRemarks` / `sessionRequireMedia`, checklist sau buổi; app GV hiện danh sách còn thiếu + nút xác nhận bài); **Lịch nhiều giai đoạn & xếp lại buổi** (`validatePhases` cho lớp nháp, “Xếp lại buổi theo lịch” neo lại cả dãy nhưng không đụng buổi đã có dữ liệu — nhận xét / bài tập / ảnh / hoàn tất, trang `/classes/kiem-tra-lich` toàn hệ thống, gợi ý tên lớp theo quy ước + mã lớp theo `max(seq)` và cho nhập tay) | Xong |
| 13D | **Chấm công & đơn từ & phân quyền theo vị trí** (bù khoảng trống so với bản gốc): **công đếm theo ca đã xếp** — lượt quét chỉ sinh cờ (20 cờ: không có lượt, thiếu lượt ra, muộn, sớm, thiếu giờ, ngoài vùng, chấm ngoài lịch…), rà cờ có kết luận (ghi nhận / gỡ / vắng có lý do), ghi đè công luôn thắng, chốt kỳ cảnh báo cờ chưa rà + công chuẩn; **danh mục ~22 mã ca gốc** (có giờ / chỉ nơi làm / linh động / nghỉ / nghỉ phép, 0,5–1,5 công, nhiều đoạn giờ, nơi làm, phải quét), **lưới phân ca tháng** có nguồn ô (khung / sửa tay / từ đơn / import) — sinh lưới và nhập Sheet không đè ô sửa tay và ô từ đơn, **khung ca tuần**, **nhập lịch dán từ Sheet (TSV/CSV)**; **10 loại đơn** theo 3 nhóm với trường riêng từng loại, 8 loại nghỉ có cờ có lương, cờ nộp muộn, cơ sở nhận đơn — **duyệt là áp ngay** (đổi ô lưới, ghi mã nghỉ / làm từ xa / công tác, thêm mốc giờ chỉnh tay, huỷ buổi dạy, gán người dạy thay) trong savepoint, áp hỏng thì đơn ở lại Chờ duyệt kèm lý do và báo người duyệt, từ chối bắt buộc lý do; **điểm chấm công QR** (mã ký HMAC + đời khoá, geofence theo điểm, /cham-cong/checkin, màn hình quầy /cham-cong/man-hinh, trang quản trị điểm); **vị trí = bộ vai trò** (positions.roles, phân công sinh user_roles có hiệu lực → hết hạn tự mất quyền) và **điều động tác nghiệp** mở phạm vi dữ liệu theo thời gian | Xong |
| 13E | **Đợt 5 — nốt các khoảng trống còn lại** (docs/GAP-NGHIEP-VU.md · docs/DOI-SANH.md mục 3, 5, 7, 9, 10, 11, 12, 14, 15): **Gói khoá học** (`/course-packages`: bảng `course_packages` — khoá, mã, cấp độ, số buổi, giá niêm yết, giá ưu đãi, mô tả marketing, nổi bật, đang bán, thứ tự; CRUD + xuất CSV; chọn gói khi **tạo đơn** tự điền số buổi + đơn giá và khi **chốt lead** tự điền số buổi); **Tổng quan điểm danh theo lớp** (`/attendance`: sĩ số, buổi đã dạy, **buổi chưa chốt điểm danh** kèm ngày cũ nhất, lọc cơ sở / trạng thái lớp, mở thẳng lưới điểm danh sẵn có); **Chi tiết ghi danh** (`/enrollments/:id`: dòng thời gian `enrollment_events`, đợt bảo lưu, yêu cầu chuyển lớp, đơn hàng & khoản thu liên quan, học phí phải đóng / đã thu / còn thiếu, đổi trạng thái + đổi số buổi có lý do); **Tab trong trang lớp** (Học bù · Ảnh lớp đã duyệt · Đánh giá & nhận xét — dùng lại service sẵn có, chỉ thêm bộ lọc theo lớp); **Xuất CSV** cho `/audit-log` và `/students/tai-khoan` kèm **gửi lại mã kích hoạt hàng loạt** (mã mới 72 giờ chỉ lưu băm, xếp vào hàng đợi thông báo ZNS — worker gửi theo giờ yên lặng và trần tin/ngày, có email thì gửi thêm email); **Ẩn / xoá mềm thông báo đã đăng** (lý do ≥5 ký tự + nhật ký, cổng phụ huynh không còn thấy) và **mẫu email mới** (Gửi đơn hàng cho khách, Mã kích hoạt tài khoản phụ huynh); **SataCoin luật thưởng xu** (bảng `coin_rules` + tab cấu hình; áp cho thưởng chuyên cần cả buổi, chấm bài tập ≥80%, lời chúc sinh nhật); **bù cột & bộ lọc** (`/payments` nguồn HV + sale phụ trách, `/rooms` trạng thái Hoạt động / Bảo trì / Tạm ngừng + thiết bị, `/quan-ly-chia-lead` lọc khu vực, `/sinh-nhat` nút "Chạy quét sinh nhật" tạo việc chăm sóc, `/orders/:id` nút "Gửi email đơn hàng"); **Dashboard** biểu đồ "Phễu lead theo tuần" 8 tuần gần nhất (SVG thuần) | Xong |
| 13F | **Đợt 6 — bám sát khảo sát gốc vòng 2, mục "Học viên, ghi danh, lớp, buổi"** (`docs/KHAO-SAT-GOC-2.md` §2): **Ảnh lớp hai tầng** (`session_media.status` thêm `library` — GV tải ảnh vào **kho của lớp**, phụ huynh chưa thấy; gắn thẻ học viên hoặc đánh dấu **ảnh chung cả lớp** (`is_class_wide`, mọi PH trong lớp xem được và cần đồng ý đăng ảnh của cả lớp) rồi **Gửi duyệt**; thêm **Ngày chụp** (`taken_at`), mốc gửi duyệt `submitted_at` để tính quá hạn 24 giờ, **tối đa 40 ảnh mỗi lô**; ảnh bị loại giữ `rejected_at` và **còn khôi phục trong 7 ngày**; màn Duyệt ảnh gom cả **buổi đã qua chưa có ảnh** kèm nút **“Buổi này không có ảnh”** (`sessions.no_media_at`) để hết cảnh báo quá hạn; bộ lọc Trong kho / Chờ duyệt / Đã duyệt / Từ chối); **Hoàn thành khoá theo luồng đề xuất** (`course_completions.status` `proposed → approved / rejected` với `proposed_by/at`, `decided_by/at`, `reject_reason`; quyền mới `completion:propose|approve|read` — giáo viên chỉ **đề xuất** lớp mình, người có quyền quản lý Duyệt / Từ chối (bắt buộc lý do), **chứng chỉ chỉ sinh khi được duyệt**; trang `/hoan-thanh-khoa` thêm khối “Đề xuất chờ duyệt”, giữ luồng hoàn thành nhiều học viên một lần); **Điểm danh ghi rõ nhu cầu học bù** (`attendance.needs_makeup` + `absence_reason`: màn điểm danh của GV và lưới điểm danh quản trị cho chọn “Cần học bù” / “Không bù” và nhập lý do phụ huynh xin vắng ngay tại dòng; “Chờ xếp bù” lọc theo `needs_makeup`, dòng cũ `null` vẫn suy diễn như trước); **Nhóm lớp** (bảng `class_groups` + `classes.class_group_id`, trang `/class-groups` — mã hiển thị, tên nhóm, cơ sở, số lớp, trạng thái, thêm / sửa / xoá mềm; ô chọn nhóm trong form sửa lớp và bộ lọc nhóm ở danh sách lớp) | Xong |
| 14 | Pilot thật tại 1 cơ sở (ký HĐĐT, Zalo ZNS, SMS, khoá VAPID), theo dõi báo cáo sau go-live 4 tuần, mở rộng cơ sở tiếp theo | Chờ quyết định cơ sở + hợp đồng |

## Việc kỹ thuật còn lại trong Giai đoạn 1 (sau khi CI xanh)

1. Supabase MFA cho SUPER_ADMIN/HO_*; middleware `proxy.ts` chặn route theo role.
2. `session_media` upload qua presigned URL (R2) + duyệt ảnh + lọc theo `media_consent`.
3. Offline queue cho điểm danh (IndexedDB + background sync) trong Teacher PWA.
4. Bảng `outbox` + worker (Inngest/Trigger.dev) phát sự kiện `session.completed` → thông báo PH.
5. RLS policies sinh từ policy engine (script) làm lớp phòng thủ cuối.
6. Trang Ops: học viên, ghi danh (UI cho `classes.enroll`), đổi lịch (đóng rule cũ + sinh lại buổi tương lai).
